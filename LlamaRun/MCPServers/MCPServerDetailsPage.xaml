<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LlamaRun.MCPServers.MCPServerDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LlamaRun.MCPServers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Theme-aware color scheme -->
        <SolidColorBrush x:Key="PageBackgroundBrush" Color="{ThemeResource SystemBaseHighColor}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{ThemeResource SystemAccentColor}"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{ThemeResource SystemAccentColorDark1}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{ThemeResource ControlFillColorDefaultBrush}"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="{ThemeResource TextFillColorPrimaryBrush}"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{ThemeResource TextFillColorSecondaryBrush}"/>
        <SolidColorBrush x:Key="TertiaryTextBrush" Color="{ThemeResource TextFillColorTertiaryBrush}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{ThemeResource ControlStrokeColorDefaultBrush}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{ThemeResource SystemFillColorSuccessBrush}"/>

        <!-- Modern card style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Info card style -->
        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <!-- Modern button style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource ButtonPointerOverBackgroundThemeBrush}"/>
            <!--<Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>-->
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>

        <!-- Tag style -->
        <Style x:Key="TagStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource AccentAcrylicBackgroundFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="0,0,8,4"/>
        </Style>

        <!-- Typography styles -->
        <Style x:Key="HeroTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="TextWrapping" Value="WrapWholeWords"/>
        </Style>

        <Style x:Key="HeroDescription" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Setter Property="LineHeight" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
            <Setter Property="TextWrapping" Value="WrapWholeWords"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="InfoLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="CharacterSpacing" Value="100"/>
        </Style>

        <Style x:Key="InfoValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Background="Transparent" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      ZoomMode="Disabled">
            <StackPanel Spacing="0" 
                        Padding="40,32,40,40" 
                        MinWidth="600"
                        HorizontalAlignment="Stretch">

                <!-- Responsive Container -->
                <Grid HorizontalAlignment="Stretch" MaxWidth="1200">
                    <StackPanel Spacing="0">

                        <!-- Hero Section with Glassmorphism Effect -->
                        <Border ActualThemeChanged="Card_Border_ActualThemeChanged" Loaded="Card_Border_Loaded" Style="{StaticResource ModernCard}" Margin="0,0,0,32">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.8">
                                    <GradientStop Color="#1E1E1E" Offset="0"/>
                                    <GradientStop Color="#2D2D30" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Server Title -->
                                <TextBlock x:Name="ServerNameTextBlock" 
                                   Grid.Row="0"
                                   Style="{StaticResource HeroTitle}"
                                   Text="Server Name"/>

                                <!-- Server Description -->
                                <TextBlock x:Name="ServerDescriptionTextBlock" 
                                   Grid.Row="1"
                                   Style="{StaticResource HeroDescription}"
                                   Text="This is a comprehensive description of what this MCP server does and how it can benefit your workflow."/>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
                                    <Button Style="{StaticResource PrimaryButton}" Click="Install_Button_Click">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE896;" FontSize="16"/>
                                            <TextBlock Text="Install"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Quick Info Cards Grid -->
                        <Grid Margin="0,0,0,32">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" MinWidth="200"/>
                                <ColumnDefinition Width="*" MinWidth="200"/>
                                <ColumnDefinition Width="*" MinWidth="200"/>
                            </Grid.ColumnDefinitions>

                            <!-- Author Card -->
                            <Border Grid.Column="0" Style="{StaticResource InfoCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource InfoLabel}" Text="Author"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <PersonPicture Width="24" Height="24"/>
                                        <TextBlock x:Name="ServerAuthorTextBlock" 
                                           Style="{StaticResource InfoValue}"
                                           Text="Author Name"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Category Card -->
                            <Border Grid.Column="1" Style="{StaticResource InfoCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource InfoLabel}" Text="Category"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8FD;" 
                                          FontSize="16" 
                                          Foreground="{ThemeResource SystemAccentColor}"
                                          VerticalAlignment="Center"/>
                                        <TextBlock x:Name="ServerCategoryTextBlock" 
                                           Style="{StaticResource InfoValue}"
                                           Text="Development"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Tags Card -->
                            <Border Grid.Column="2" Style="{StaticResource InfoCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource InfoLabel}" Text="Tags"/>
                                    <TextBlock x:Name="ServerTagsTextBlock" 
                                       Style="{StaticResource InfoValue}"
                                       Text="AI, Development, Productivity"
                                       TextWrapping="Wrap"
                                       TextTrimming="CharacterEllipsis"
                                       MaxLines="2"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Documentation Section -->
                        <Border Style="{StaticResource ModernCard}" MinHeight="600" Margin="0,0,0,-10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Section Header with Icon -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,20">
                                    <FontIcon Glyph="&#xE8A5;" 
                                      FontSize="20" 
                                      Foreground="{ThemeResource SystemAccentColor}"
                                      VerticalAlignment="Center"/>
                                    <TextBlock Style="{StaticResource SectionHeader}" 
                                       Margin="0,0"
                                       Text="Documentation"
                                       VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Documentation Content with Modern Styling -->
                                <Border Grid.Row="1" 
                                Background="{ThemeResource ControlFillColorDefaultBrush}"
                                CornerRadius="8"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1">
                                    <local:ReadmeViewer x:Name="ReadmeViewer"/>
                                </Border>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>